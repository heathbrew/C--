#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main(){
    string s;
    // getline(cin,s);
    // stringstream ss(s);
    // while(ss >> word){
    //     if(word.size()%2!=0){
    //         cout << word << endl;
    //     }
    // }
    while(getline(cin,s,' ')){
        if (s.size()%2!=0){
            cout << s << endl;
        }
    }
    // cout << s;
    // for(int i = 0;i<s.size())
    
}